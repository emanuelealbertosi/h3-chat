diff --git a/include/engine/models/yue2/types.h b/include/engine/models/yue2/types.h
index a3c9f57..0e6c8e0 100644
--- a/include/engine/models/yue2/types.h
+++ b/include/engine/models/yue2/types.h
@@ -72,6 +72,8 @@ enum class Yue2CotMode {
 };
 
 struct Yue2Request {
+    std::string h3_artifact_dir;
+    bool h3_plan_only = false;
     std::string style;
     std::string lyrics;
     Yue2CotMode cot = Yue2CotMode::Full;
diff --git a/src/models/yue2/pipeline.cpp b/src/models/yue2/pipeline.cpp
index 713dd62..03fbd81 100644
--- a/src/models/yue2/pipeline.cpp
+++ b/src/models/yue2/pipeline.cpp
@@ -9,6 +9,8 @@
 #include <cstdint>
 #include <stdexcept>
 #include <utility>
+#include <fstream>
+#include <filesystem>
 
 namespace engine::models::yue2 {
 namespace {
@@ -173,6 +175,7 @@ public:
             ar->release_runtime_graphs();
         }
         ensure_ar();
+        if (request.h3_plan_only) return out;
         const auto negative_start = Clock::now();
         const auto neg = negative_prefix(request, tokenizer, out.plan.abc_ids);
         engine::debug::timing_log_scalar("yue2.semantic.negative_prefix_ms", engine::debug::elapsed_ms(negative_start));
@@ -312,9 +315,29 @@ public:
         engine::debug::timing_log_scalar("yue2.plan_ms", engine::debug::elapsed_ms(plan_start, Clock::now()));
         const auto semantic_start = Clock::now();
         auto semantic = generate_semantic(request, std::move(planned));
+        if (!request.h3_artifact_dir.empty()) {
+            const auto dir = std::filesystem::u8path(request.h3_artifact_dir);
+            std::filesystem::create_directories(dir);
+            auto save_ids = [&dir](const char *name, const std::vector<int32_t> &ids) {
+                std::ofstream file(dir / name, std::ios::binary);
+                file.write(reinterpret_cast<const char *>(ids.data()), ids.size() * sizeof(int32_t));
+                if (!file) throw std::runtime_error("H3-Music artifact write failed");
+            };
+            save_ids("abc_tokens.i32", semantic.plan.abc_ids);
+            save_ids("semantic_tokens.i32", semantic.tokens);
+            std::ofstream(dir / "generation_flags.json") << "{\"abc_truncated\":"
+                << (semantic.plan.truncated ? "true" : "false") << ",\"audio_truncated\":"
+                << (semantic.truncated ? "true" : "false") << "}";
+        }
+        if (request.h3_plan_only) return runtime::AudioBuffer{48000, 2, {}};
         engine::debug::timing_log_scalar("yue2.semantic_ms", engine::debug::elapsed_ms(semantic_start, Clock::now()));
         const auto nar_start = Clock::now();
         auto latents = synthesize_latents(semantic, request.generation, request.nar_noise, request.seed);
+        if (!request.h3_artifact_dir.empty()) {
+            std::ofstream file(std::filesystem::u8path(request.h3_artifact_dir) / "latents.f32", std::ios::binary);
+            file.write(reinterpret_cast<const char *>(latents.data()), latents.size() * sizeof(float));
+            if (!file) throw std::runtime_error("H3-Music latent write failed");
+        }
         engine::debug::timing_log_scalar("yue2.nar_ms", engine::debug::elapsed_ms(nar_start, Clock::now()));
         const int64_t frames = static_cast<int64_t>(latents.size()) / assets->config.model.latent_dim;
         ar.reset();
diff --git a/src/models/yue2/request.cpp b/src/models/yue2/request.cpp
index e6e547e..3c6643d 100644
--- a/src/models/yue2/request.cpp
+++ b/src/models/yue2/request.cpp
@@ -55,6 +55,8 @@ std::string abc_from_options(const std::unordered_map<std::string, std::string>
 void apply_options(
     Yue2Request & out,
     const std::unordered_map<std::string, std::string> & options) {
+    out.h3_artifact_dir = runtime::find_option(options, {"h3_artifact_dir"}).value_or("");
+    out.h3_plan_only = runtime::find_option(options, {"h3_plan_only"}).value_or("false") == "true";
     if (const auto cot = runtime::find_option(options, {"cot"})) {
         out.cot = parse_cot_mode(*cot);
     }
